export default function ComingSoonChart({ title, reason }) {
    return (
      <div className="chart-placeholder">
        <p className="chart-placeholder__title">{title}</p>
        <p className="chart-placeholder__reason">{reason}</p>
      </div>
    );
  }